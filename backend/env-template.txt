# Server Configuration
PORT=3000

# Environment Mode
NODE_ENV=production
TESTING=false

# Database Configuration - MongoDB Atlas
# Get your connection string from: https://cloud.mongodb.com
# Format: mongodb+srv://username:password@cluster-name.xxxxx.mongodb.net/resume-tracker
# Example:
# MONGODB_URI=mongodb+srv://admin:yourpassword@cluster0.abc123.mongodb.net/resume-tracker?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/resume-tracker?retryWrites=true&w=majority

# For local development (optional):
# MONGODB_URI=mongodb://localhost:27017/resume-tracker

# Admin Configuration
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_SESSION_SECRET=your-secret-key-change-in-production

# Email Configuration (REQUIRED for production)
# You MUST configure these for production to enable email functionality
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Note: 
# - PORT: Server port (defaults to 3000 if not set)
# - NODE_ENV: Set to 'development' or 'production'
# - TESTING: Set to 'false' for production (enables real email authentication)
# - EMAIL_USER: Your Gmail address for sending magic links (REQUIRED for production)
# - EMAIL_PASS: Your Gmail app password (REQUIRED for production)

